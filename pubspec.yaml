name: project_07_chat

description: A simple chat app

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  dio: ^5.4.3

# No additional dependencies required for basic Material UI and navigation.

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
